Takes two input csv files and returns a dictionary with the sum of those csv files.
Sum is calculated as it follows:
  sums 3rd and 4th column of one line if 1st column is the same(in both files)
  else adds the line in dictionary s
